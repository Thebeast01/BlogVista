import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['github.com', 'res.cloudinary.com'], // Add your image domains here

  }
  /* config options here */
};

export default nextConfig;

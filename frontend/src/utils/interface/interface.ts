export interface ModelProps {
  x: string;
  onClose: () => void;
}
export interface blogInterface {
  title: string;
  id: string;
  content: JSON;
  coverImg: string;
  authorId: string;
  createdAt: string;
}
